{% if theme is defined %}
    {% form_theme form theme %}
{% endif %}

{{ form_widget(form) }}

<script type='text/javascript'>
$(function() {
    $("input[data-toggle='datepicker']").datepicker({language: '{{ app.request.locale }}'});
})
</script>
